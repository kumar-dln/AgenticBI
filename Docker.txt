FROM public.ecr.aws/lambda/python:3.12

# Install matplotlib directly into the container task root
RUN pip install --no-cache-dir \
matplotlib \
numpy \
pandas \
strands-agents \
strands-agents-tools

# Copy your actual script file into the task root directory
COPY lambda_function.py ${LAMBDA_TASK_ROOT}

# Tell AWS Lambda which handler function to trigger
CMD [ "lambda_function.lambda_handler" ]